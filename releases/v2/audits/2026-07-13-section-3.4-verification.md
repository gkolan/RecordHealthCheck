# Section 3.4 verification — Required pack contract

## Scope

`V2-RELEASE-PLAN.md` §3.4: `example.yml` contract and ten-section README per pack.

## Method

```text
npm run validate --prefix RecordHealthCheck-Examples
for p in packs/*/README.md; do rg -c "^## [0-9]+\." "$p"; done
test -f docs/manifest-schema.json
```

## Findings

| Item                            | Status | Evidence                                                    |
| ------------------------------- | ------ | ----------------------------------------------------------- |
| `example.yml` on every pack     | Pass   | 5/5 packs                                                   |
| JSON Schema present             | Pass   | `docs/manifest-schema.json`                                 |
| README ten sections             | Pass   | Sections 1–10 in each pack README                           |
| `CHANGELOG.md`                  | Pass   | Each pack                                                   |
| `destructiveChanges.xml`        | Pass   | Each pack `manifest/`                                       |
| Honest `removal.status`         | Pass   | `documented`, `partial`, `not-supported` used appropriately |
| `maximumTestedVersion` separate | Pass   | `compatibility.core.maximumTestedVersion` in all manifests  |

## Verdict

**Pass** for pack contract on migrated packs.
