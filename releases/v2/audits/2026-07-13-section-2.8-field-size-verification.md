# Section 2.8 field-size verification

## Scope

`V2-RELEASE-PLAN.md §2.8`, all shipped Check Set and Rule custom metadata fields.

## Method

Ran `python3 releases/v2/tools/generate_field_size_registry.py` from the repository root and inspected the generated registry against the field XML.

## Findings

| Item                                     | Status | Evidence                                                                                                         | Owner |
| ---------------------------------------- | ------ | ---------------------------------------------------------------------------------------------------------------- | ----- |
| Registry generated from shipped metadata | Pass   | `docs/reference/field-size-registry.md`; **53** fields (includes `PublishRunEvent__c` / `PublishResultEvent__c`) | Core  |
| Required columns                         | Pass   | Storage type, stored maximum, post-resolution maximum, overflow behavior                                         | Core  |
| `ActionUrl__c` post-resolution limit     | Pass   | Stored Long Text Area 32,768; post-resolution maximum **2,000** characters; overflow → error                     | Core  |
| Re-runnable source of truth              | Pass   | `releases/v2/tools/generate_field_size_registry.py`                                                              | Core  |

## Outstanding work

None for §2.8.

## Verdict

Pass — reconciled to the 2,000-character URL limit and regenerated for all 53 current fields.
